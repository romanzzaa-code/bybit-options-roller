Context: Bybit Options Roller (Go)Дата сохранения: 11.01.2026Статус: Этап 1 (Core) завершен. Переход к Этапу 2 (Database).1. Описание проектаSaaS-бот для автоматического роллирования опционных позиций на Bybit (Unified Trading Account).Архитектура: Modular Monolith / Clean Architecture.Стек: Go 1.22+, Bybit V5 API, PostgreSQL (планируется).2. Текущая структура проектаПроект разбит на слои согласно принципам Clean Architecture:cmd/bot/: Точка входа (main.go). Инициализирует конфиг, клиент API и сервис. Содержит тестовый прогон (manual test).internal/domain/:models.go: Сущности (Task, User, Position) и Value Objects. Используется decimal.Decimal для денег.interfaces.go: Контракты (ExchangeAdapter, TaskRepository). Вся логика зависит от них.internal/infrastructure/bybit/:client.go: Реализация ExchangeAdapter. Подпись запросов (HMAC-SHA256), методы GetMarkPrice, GetPosition, PlaceOrder.dto.go: Структуры для парсинга JSON ответов от API V5.internal/usecase/:roller.go: Бизнес-логика роллирования.Проверка триггера (Call/Put).Паттерн Saga (Закрытие Leg 1 -> Открытие Leg 2).Парсинг и расчет следующего тикера (Regex).internal/config/: Загрузка конфигурации (пока заглушка для локального запуска).3. Последние действияРеализована вся базовая логика "Ядра".Выполнен go run cmd/bot/main.go.Результат теста: Успешная инициализация. Клиент отправил запрос на Bybit Testnet. Получена ошибка symbol not found (ожидаемо, так как тестовый тикер устарел), что подтверждает работоспособность сетевого слоя и обработки ошибок.4. План на следующую сессию (Этап 2)База данных:Поднять PostgreSQL в Docker.Написать SQL-миграции (создание таблиц).Repository Layer:Реализовать TaskRepository (методы GetTask, UpdateStatus).Интеграция:Заменить фейковые данные в main.go на чтение из реальной БД.5. Заметки разработчикаВ infrastructure/bybit/client.go сейчас используется прямая передача секрета. При внедрении БД нужно добавить дешифровку (SecretEnc).Логика роллирования (roller.go) готова к тестированию на реальных тикерах

# Context Update: Architecture Refactoring & Domain Fixes
**Date:** 11.01.2026
**Status:** In Progress (Transition to Event-Driven)
**Reviewer:** Code Critic (Senior Architect)

## 1. Суть изменений (Summary)
Был проведен глубокий рефакторинг ядра системы (Core Domain & Architecture) для устранения критических рисков потери средств.

### 🔴 Выявленные критические проблемы (Legacy Code):
1.  **Polling Architecture:** Использование `sleep(30s)` и перебор всех задач в цикле. Риск: пропуск цены, бан по IP от биржи.
2.  **Logic Error:** Бот следил за ценой *Опциона*, а не *Базового Актива*. Риск: полная неработоспособность стратегии.
3.  **No Atomicity:** Отсутствие транзакционности между закрытием Leg 1 и открытием Leg 2. Риск: "голая" позиция при сбое.
4.  **Race Conditions:** Отсутствие блокировок при работе нескольких инстансов/воркеров.

### ✅ Внесенные изменения (Refactoring Results):

#### Domain Layer (`internal/domain/`)
* **Новая модель `Task`:**
    * Добавлено поле `UnderlyingSymbol` (например, "BTC") для корректного трекинга триггера.
    * Добавлено поле `Version` для Optimistic Locking.
* **Value Objects:** Внедрен `OptionSymbol` с безопасным парсингом и математикой страйков.
* **Logic:** Метод `ShouldRoll` перенесен внутрь модели Task (Rich Domain Model).

#### Infrastructure Layer (`internal/infrastructure/`)
* **Database:**
    * Миграция: добавлены поля `underlying_symbol`, `version`.
    * Репозиторий: методы `UpdateTaskState` и `UpdateTaskSymbol` теперь используют `WHERE id=? AND version=?`.
* **Bybit Client:**
    * Реализован метод `GetIndexPrice` (через Linear Perpetual Tickers) для получения быстрой и точной цены БА.

#### UseCase Layer (`internal/usecase/`)
* **RollerService:**
    * Полностью переписан на Saga Pattern (State Machine).
    * Этапы: `IDLE` -> `ROLL_INITIATED` -> `LEG1_CLOSED` -> `LEG2_OPENING` -> `IDLE`.
    * Состояние сохраняется в БД на каждом шаге.

#### Entry Point (`cmd/bot/`)
* Удален наивный цикл `processTasks`.
* Приложение подготовлено к внедрению `Event Engine` (WebSocket).

---

## 2. Текущее состояние файлов

| Файл | Статус | Примечание |
|---|---|---|
| `domain/models.go` | ✅ Stable | Rich Model, State Machine |
| `domain/interfaces.go` | ✅ Stable | Updated contracts |
| `infrastructure/database/repository.go` | ✅ Stable | Optimistic Locking implemented |
| `infrastructure/bybit/client.go` | ✅ Stable | Added GetIndexPrice |
| `usecase/roller.go` | ✅ Stable | Saga Pattern implemented |
| `cmd/bot/main.go` | 🚧 W.I.P. | Services initialized, waiting for Event Loop |
| `migrations/001_initial_schema.sql` | ✅ Stable | Updated schema |

## 3. Следующие шаги (Next Actions)
1.  **Implement Engine:** Создать `internal/engine/market_stream.go` (WebSocket Client).
2.  **Implement Dispatcher:** Создать распределитель задач (Map: Symbol -> TaskID).
3.  **Implement Recovery:** Написать воркер, который при старте ищет задачи в статусе `LEG1_CLOSED`..

# Context: Bybit Options Roller (Go SaaS)

## 1. Текущий статус проекта
**Статус:** Production Ready Candidate (v1.0)
**Архитектура:** Modular Monolith / Clean Architecture
**Язык:** Go 1.22+

## 2. Ключевые архитектурные решения

### 2.1. Domain-Driven Design (DDD)
* **Изоляция домена:** Логика формирования тикеров (например, добавление суффикса `USDT` к базовому активу) вынесена в доменный слой (`internal/domain/symbol.go`). Инфраструктурный слой (HTTP клиент) очищен от бизнес-правил.
* **Rich Model:** Сущность `Task` инкапсулирует логику проверки триггеров (`ShouldRoll`), но с возможностью обхода для восстановления состояния.

### 2.2. Надежность и Отказоустойчивость (Reliability)
* **Идемпотентность:** `OrderLinkID` генерируется на основе ID задачи и **версии** (`fmt.Sprintf("close-%d-v%d", task.ID, task.Version)`). Это защищает от дублирования ордеров при ретраях.
* **Optimistic Locking:** Обновление состояния задачи в БД требует совпадения версии (`WHERE id = $1 AND version = $2`). Это предотвращает гонки данных между воркерами.
* **Crash Recovery (Восстановление):** Реализован механизм "Short-circuit" в `ExecuteRoll`. Если задача находится в статусе `LEG1_CLOSED` (первая нога закрыта, но произошел сбой), бот игнорирует проверки цены и немедленно переходит к открытию второй ноги, чтобы закрыть "голую" позицию.
* **Transient Error Handling:** Репозиторий (`RegisterError`) распознает временные ошибки (Timeout, 502/504) и не переводит задачу в фатальный статус `FAILED`, позволяя воркеру повторить попытку.

### 2.3. Наблюдаемость (Observability)
* **Structured Logging:** Используется `log/slog` (JSON format).
* **Contextual Logging:** Логгер обогащается атрибутами (`task_id`, `symbol`) внутри сервисов для упрощения отладки в Kibana/Loki.

### 2.4. Конфигурация и Безопасность
* **Timeouts:** HTTP-таймауты для Bybit API конфигурируются через ENV переменные и передаются в клиент.
* **Dependency Injection:** Зависимости внедряются явно через конструкторы. Репозитории задач очищены от ненужных зависимостей (encryptor удален из `TaskRepo`).

## 3. Структура модулей

### `cmd/bot`
Точка входа. Инициализирует:
* `slog` (JSON Handler).
* Config & DB Connection.
* Clients & Repositories.
* `WorkerManager` с Graceful Shutdown.

### `internal/worker/manager.go`
* Запускает фоновый цикл обработки задач.
* Использует `sync.WaitGroup` для ожидания завершения активных роллов при остановке сервиса.
* Выбирает задачи пачками (Batch processing).

### `internal/usecase/roller.go`
Ядро логики роллирования:
1.  **Check Resume:** Если статус `LEG1_CLOSED` -> немедленный `Leg 2`.
2.  **Check Trigger:** Если статус `IDLE` -> проверка цены через `ShouldRoll`.
3.  **Execution:**
    * Lock Task (Optimistic).
    * Close Leg 1 -> Save State.
    * Open Leg 2 -> Save State.
    * Finalize (Update Symbol -> IDLE).

## 4. Технический долг и планы (v1.1)
* [ ] **Side Handling in Recovery:** В логике восстановления жестко прописан `SideSell`. Для поддержки Long-стратегий нужно сохранять сторону сделки в контексте задачи.
* [ ] **Dynamic Quote Currency:** Логика `GetIndexTicker` предполагает `USDT`. При добавлении USDC/Coin-M контрактов потребуется рефакторинг.
* [ ] **Alerting:** Подключить `NotificationService` (Telegram) для критических ошибок (`TaskStateFailed`).


# Dev Log: Bybit Options Roller Setup
**Дата:** 15.01.2026
**Статус:** ✅ Система запущена и работает (Event-Driven Mode)

## 1. Конфигурация окружения (.env)
Для локального запуска был создан файл `.env` в корне проекта.
Используется библиотека `github.com/joho/godotenv/autoload` для автоматической подгрузки.

**Структура `.env`:**
```env
# Database (должно совпадать с docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_USER=bybit_roller
DB_PASSWORD=secret_password
DB_NAME=bybit_roller

# Security
# Ключ шифрования (AES-256). Должен быть в формате HEX (64 символа = 32 байта).
# Генерация: openssl rand -hex 32
ENCRYPTION_KEY=d8e8fca2dc0f896fd7cb4cb0031ba249ba068b556b0c98eed6c68b8e8f80455c

# Bybit
BYBIT_TESTNET=true
# BYBIT_TIMEOUT_SECONDS=5 (optional)
2. Инфраструктура (Docker & DB)
База данных запускается через Docker Compose.

Команды:

Bash
# Запуск БД
docker-compose up -d postgres

# Сброс БД (удаление данных) при необходимости
docker-compose down -v
Миграции

Автоматическая миграция при старте приложения отключена. Схема была применена вручную.

Применение схемы (One-off):

Bash
cat migrations/001_initial_schema.sql | docker exec -i bybit-roller-db psql -U bybit_roller -d bybit_roller
3. Исправления в коде (Refactoring)
A. cmd/bot/main.go

Проблема: Использование os.Getenv вместо загрузчика конфига, отсутствие загрузки .env. Решение:

Добавлен импорт _ "github.com/joho/godotenv/autoload".

Заменен ручной сбор конфига на config.LoadConfig().

Добавлена корректная передача параметров в database.NewConnection.

B. cmd/seeder/main.go

Проблема: Нарушение Foreign Key (попытка создать Task без User и APIKey). Решение: Полностью переписан main сидера. Теперь он создает данные иерархически:

User (TelegramID: 12345)

APIKey (привязан к User)

Task (привязан к User и APIKey, с актуальным символом опциона)

C. Зависимости

Добавлена библиотека godotenv:

Bash
go get [github.com/joho/godotenv](https://github.com/joho/godotenv)
4. Запуск и Проверка
1. Генерация данных (Seeding):

Bash
go run cmd/seeder/main.go
# Ожидаемый результат: ✅ Task created! ID: ...
2. Запуск бота:

Bash
go run cmd/bot/main.go
Текущее поведение: Бот запускается, подключается к БД, находит задачу и подписывается на WebSocket Bybit. В логах:

INFO Starting Manager: Event-Driven Mode

INFO Connecting to Bybit Stream...

Примечание: Бот "молчит" в логах после старта, так как ждет событий (сделок/изменения цены) от биржи. На Testnet активность низкая, это нормальное поведение.